# Code Plan: Issue #24 Docs Refresh for JSON-First Pipeline

## Objective

Refresh architecture and operator-facing docs to match current implementation state after JSON-first migration and run-manifest artifact support.

## Scope

- update `docs/ARCHITECTURE.md` data-flow and state sections
- update `docs/DAT_SOURCES.md` provider handling details
- update `docs/PROVIDER_NORMALIZATION.md` semantic mapping behavior

## Validation

- ensure docs reflect implemented services and outputs
- ensure references point to active docs (`JSON_SCHEMA`, `CONVERSION_STRATEGY`, `SYNC_MANIFESTS`)
